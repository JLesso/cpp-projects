Doubly Linked List Name Manager

This project implements a doubly linked list in C++.

What it does

Reads names from input.txt
Converts all input to lowercase
Inserts names in sorted order
Deletes a name when a line starts with delete
Writes the final list to output.txt in ascending order
Writes a separator line
Writes the list again in descending order