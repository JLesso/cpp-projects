This project implements a max heap in C++ using a linked binary tree structure instead of an array.

What it does

Reads integer values from input.txt
Builds a max heap by inserting each value
Prints the heap level by level to output.txt
Deletes the maximum value from the heap
Prints the heap again
Deletes the maximum value one more time
Prints the heap again